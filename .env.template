# Environment Configuration Template
# Copy this file to .env and update with your actual values

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=123456789012

# S3 Bucket Configuration
S3_BUCKET_NAME=dex-trigger-archive

# Database Secret Configuration
# Create a secret in AWS Secrets Manager with the following JSON structure:
# {
#   "host": "your-rds-endpoint.rds.amazonaws.com",
#   "username": "db_user",
#   "password": "db_password",
#   "database": "Mediant",
#   "port": 1433
# }
DB_SECRET_NAME=dex-archive/db-credentials

# API Secret Configuration
# Create a secret in AWS Secrets Manager with the following JSON structure:
# {
#   "token_url": "https://your-oauth-server.com/oauth/token",
#   "client_id": "your_client_id",
#   "client_secret": "your_client_secret"
# }
API_SECRET_NAME=dex-archive/api-credentials

# DEX API Configuration
DEX_API_BASE_URL=https://api.example.com

# VPC Configuration (optional - leave empty if not using VPC)
VPC_SECURITY_GROUP_IDS=
VPC_SUBNET_IDS=

# Stack Name for CloudFormation
STACK_NAME=dex-trigger-archive-stack
