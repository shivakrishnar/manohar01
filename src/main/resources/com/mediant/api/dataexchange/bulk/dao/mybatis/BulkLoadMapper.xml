<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mediant.api.dataexchange.bulk.dao.BulkLoadMapper">
  <!-- Placeholder MyBatis mapper for Bulk load operations -->
  <select id="selectPendingLoads" resultType="map">
    SELECT * FROM dbo.FileLoadQueue WHERE Processed = 0
  </select>
</mapper>
